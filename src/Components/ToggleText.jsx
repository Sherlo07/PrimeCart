/*import React, { useState } from "react";

const ToggleText = () => {
  const [visible, setVisible] = useState(false);

  return (
    <>
    <div className="">
        
      <button className="border-2 border-black rounded-lg" onClick={() => setVisible(!visible)}>
        {visible ? "Hide" : "Show"} Text
      </button>
      {visible && <p>This is some hidden text!</p>}
    </div>
    </>
  );
};

export default ToggleText;*/